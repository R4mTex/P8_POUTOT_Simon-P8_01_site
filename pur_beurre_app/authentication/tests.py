from django.test import TestCase
from authentication.scripts.authentication.account_exist import Account_does_he_exist

# Create your tests here.
"""
def test_should_return_false_at_account_existing():
    sut = Account_does_he_exist()
    account_test = {
        'username': 'Bob',
        'email': 'Bob@bob.com',
        'password': 'Boby'
    }
    username = '123'
    email = 'Roger'
    password = 'test@test.com'
    expected_value = False
    assert sut.account_existing(username, email, password) == expected_value

def test_should_return_true_at_account_existing():
    sut = Account_does_he_exist()
    username = '123'
    email = 'Roger'
    password = 'test@test.com'
    expected_value = False
    assert sut.account_existing(username, email, password) == expected_value
"""